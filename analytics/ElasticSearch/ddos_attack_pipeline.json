{
    "description": "Transparent Security DDOS Attack detection pipeline for ElasticSearch",
    "processors": [
        {
            "script":{
                        "lang": "painless",
                        "source":"if(ctx.ts.IPvDestAddr.value_count > (int)100 && ctx.ts.UDP2DstPort.value_count > (int)100 && ctx.ts.INTMetadataSourceMetadataOriginatingMac.value_count > (int)100 ){ ctx.is_ddos_attack = true }"
                }
        }
    ]
}
